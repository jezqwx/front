export default function Profile(){

    // throw new Error("Profile page crashed test")

    return(
        <div>
            <h1>Profile</h1>
            <p>Lazy loaded page</p>
        </div>
    )
}